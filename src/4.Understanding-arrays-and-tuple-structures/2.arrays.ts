type Names = string [];
type Identifers = Array<number>;

type BinaryDigits  =  (0|1)[];

type StringArray = string[];
type StringRecord = {[key:number]:string};

type Flags = boolean[];
type FlageType = Flags[number];  // type is boolean
