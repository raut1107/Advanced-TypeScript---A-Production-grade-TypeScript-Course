type CompletedStates = "Success" | "Failed";
type PendingStates = "Loading" | "Idle";
type States = PendingStates | CompletedStates;

const handleAllStates = (state:States) =>{

};

const handleCompletedStates = (state:CompletedStates) =>{
    
}